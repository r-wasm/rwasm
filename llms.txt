# rwasm

## Build R Packages for WebAssembly

This package provides functions to help build R packages compiled for
WebAssembly (Wasm), manage Wasm binary R package libraries and
repositories, and prepare webR compatible filesystem images for static
web hosting of data files and R packages.

## Requirements

Building binary R packages for Wasm requires cross-compiling packages
containing C/C++/Fortran source code using a Wasm development toolchain.
As such, the [Emscripten](https://emscripten.org) C/C++ toolchain and a
[version of LLVM `flang` configured to output
Wasm](https://github.com/lionel-/f18-llvm-project/tree/fix-webr) must be
available in the environment for the `rwasm` package to function.

For convenience, webR GitHub repository provides a [Docker
container](https://github.com/r-wasm/webr/pkgs/container/webr)
containing the full Wasm development environment required for building
Wasm R packages.

## Installation

The `rwasm` package is not yet on CRAN. You can install the development
version from GitHub via:

``` r
# install.packages("pak")
pak::pak("r-wasm/rwasm")
```

Further information explaining how to set up the Wasm development
environment and build R packages can be found in
[`vignette("rwasm")`](https://r-wasm.github.io/rwasm/articles/rwasm.md).

# Package index

## All functions

- [`add_list()`](https://r-wasm.github.io/rwasm/reference/add_list.md) :
  Add one or more packages from a file

- [`add_pkg()`](https://r-wasm.github.io/rwasm/reference/add_pkg.md) :
  Add R package reference(s) to a package repository

- [`add_repo()`](https://r-wasm.github.io/rwasm/reference/add_repo.md) :
  Add all packages from a CRAN-like repository to a package repository

- [`add_tar_index()`](https://r-wasm.github.io/rwasm/reference/add_tar_index.md)
  :

  Add Emscripten virtual filesystem metadata to a given `tar` archive

- [`build()`](https://r-wasm.github.io/rwasm/reference/build.md) : Build
  one or more R packages for WebAssembly

- [`file_packager()`](https://r-wasm.github.io/rwasm/reference/file_packager.md)
  : Create an Emscripten filesystem image

- [`make_library()`](https://r-wasm.github.io/rwasm/reference/make_library.md)
  : Create an R package library

- [`make_vfs_library()`](https://r-wasm.github.io/rwasm/reference/make_vfs_library.md)
  : Create an Emscripten filesystem image of an R package library

- [`make_vfs_repo()`](https://r-wasm.github.io/rwasm/reference/make_vfs_repo.md)
  : Add Emscripten filesystem images to an R package repository

- [`rm_pkg()`](https://r-wasm.github.io/rwasm/reference/rm_pkg.md) :
  Remove R package(s) from a package repository

- [`write_packages()`](https://r-wasm.github.io/rwasm/reference/write_packages.md)
  :

  Write the `PACKAGES` file for a package repository

# Articles

### All vignettes

- [Build R packages using GitHub
  Actions](https://r-wasm.github.io/rwasm/articles/github-actions.md):
- [Mounting filesystem
  images](https://r-wasm.github.io/rwasm/articles/mount-fs-image.md):
- [Mounting host directories in
  node](https://r-wasm.github.io/rwasm/articles/mount-host-dir.md):
- [Get started with
  rwasm](https://r-wasm.github.io/rwasm/articles/rwasm.md):
- [Technical details for .tar archive
  metadata](https://r-wasm.github.io/rwasm/articles/tar-metadata.md):
